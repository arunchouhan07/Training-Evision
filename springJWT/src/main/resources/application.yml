spring:
  application:
    name: springJWT
  datasource:
    url: jdbc:mysql://localhost:3306/spring_jwt
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
